{
  "name": "yutubo-backend",
  "version": "1.0.0",
  "description": "Yutubo backend API — yt-dlp powered YouTube downloader",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "eslint . --ext .js",
    "typecheck": "echo \"No TypeScript in this project — skipping typecheck\" && exit 0",
    "test": "echo \"Manual/e2e testing via curl + frontend — see README.md\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "execa": "^8.0.1",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.1",
    "youtube-po-token-generator": "^0.1.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "license": "MIT"
}
